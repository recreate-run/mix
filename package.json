{
	"name": "mix-monorepo",
	"private": true,
	"workspaces": [
		"tauri_app",
		"packages/*",
		"docs"
	],
	"scripts": {
		"dev": "ENV=development",
		"build": "npm run build --workspaces",
		"clean": "npm run clean --workspaces"
	},
	"devDependencies": {
		"typescript": "^5.6.2"
	},
	"dependencies": {
		"vercel": "^46.0.2"
	}
}
