{  
  "name": "<animation_name>",
  "description": "<animation_description>",
  "version": "1.0.0",
  "parameters": [
    {
      "name": "displayText",
      "type": "string", 
      "default": "Your Text Here",
      "_comment": "Primary text content - use 'displayText', 'overlayText', or 'singleWord' based on context"
    },
    {
      "name": "animationDurationSeconds",
      "type": "number",
      "default": 3,
      "_comment": "Total animation duration - ALWAYS use '*DurationSeconds' naming pattern"
    },
    {
      "name": "textSizeRem",
      "type": "number", 
      "default": 3,
      "_comment": "Text size in rem units - use '*SizeRem' for rem-based sizing"
    },
    {
      "name": "textColor",
      "type": "string",
      "default": "#fff",
      "_comment": "Primary text color - use '*Color' naming pattern with hex values"
    },
    {
      "name": "backgroundCSS",
      "type": "string",
      "default": "linear-gradient(135deg, #667eea 0%, #764ba2 100%)",
      "_comment": "Background style - use '*CSS' suffix for complex CSS values like gradients"
    },
    {
      "name": "aspectRatio", 
      "type": "string",
      "default": "9/16",
      "_comment": "REQUIRED: Aspect ratio in 'width/height' format (e.g., '16/9', '9/16', '4/3')"
    },
    {
      "name": "autoloop",
      "type": "boolean",
      "default": false,
      "_comment": "REQUIRED: Whether animation should loop infinitely"
    },
    {
      "name": "customDelaySeconds",
      "type": "number",
      "default": 0.5,
      "_comment": "EXAMPLE: Timing parameters - use '*DelaySeconds' naming pattern"
    },
    {
      "name": "elementSizePixels", 
      "type": "number",
      "default": 100,
      "_comment": "EXAMPLE: Size parameters - use '*SizePixels' for pixel-based sizing"
    },
    {
      "name": "primaryColor",
      "type": "string", 
      "default": "#84fab0",
      "_comment": "EXAMPLE: Color parameters - use descriptive names with 'Color' suffix"
    },
    {
      "name": "intensityMultiplier",
      "type": "number",
      "default": 1.0,
      "_comment": "EXAMPLE: Multiplier parameters - use '*Multiplier' for scaling factors"
    },
    {
      "name": "enableFeature",
      "type": "boolean",
      "default": true,
      "_comment": "EXAMPLE: Boolean flags - use descriptive names starting with verb"
    }
  ]
}