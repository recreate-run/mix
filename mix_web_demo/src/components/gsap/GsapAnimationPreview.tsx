// Stub component for web demo - GSAP animation preview is disabled
export function GsapAnimationPreview({ config }: { config?: any }) {
	// Ignore config parameter - animations are disabled in web demo
	return null;
}
