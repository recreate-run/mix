// Re-export types from template to maintain compatibility
export type { VideoElement, VideoConfig } from '@remotion-shared/DynamicVideoComposition';

// Alias for backward compatibility with existing code
export type RemotionVideoConfig = VideoConfig;