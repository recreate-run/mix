Use multiple parallel subagents and finds the actual request,response schema of all endpoints in @mix_agent/internal/http/. Verify if all endpoints are specified in @mix_agent/internal/http/rest_docs.go with the correct request/response schema etc. Just give a report at the
end, don't edit any code. think
