---
title: "Installation"
description: "How to install the Mix SDK"
---

import { Github } from "lucide-react";

# Installation

Installing the Mix SDK is straightforward. You can use npm, yarn, or pnpm to add it to your project. The SDK is [open source on GitHub](https://github.com/recreate-run/mix-typescript-sdk).

## Install with npm

```bash
npm install mix-typescript-sdk
```

## Install with yarn

```bash
yarn add mix-typescript-sdk
```

## Install with pnpm

```bash
pnpm add mix-typescript-sdk
```

## Source Code

You can find the source code and contribute to the SDK on GitHub:

<a 
  href="https://github.com/recreate-run/mix-typescript-sdk" 
  target="_blank" 
  rel="noopener noreferrer"
  className="inline-flex items-center gap-1.5 px-4 py-2 rounded-md bg-secondary text-secondary-foreground hover:bg-secondary/90 transition-colors mt-2"
>
  <Github className="w-4 h-4" /> recreate-run/mix-typescript-sdk
</a>

## Dependencies

The SDK has minimal dependencies and is designed to work in both Node.js and browser environments. It has built-in TypeScript types, so no separate type definitions are needed.

## Next Steps

Once you've installed the SDK, you can start using it by creating a client. See [Client Creation](/docs/sdk/client) for more details.