---
title: "Typescript SDK"
description: "TypeScript client library for building AI agents with Mix. Type-safe SDK with full IntelliSense support."
---


## Installation

```bash
npm add mix-typescript-sdk
```

## Quickstart

```typescript
import { Mix } from 'mix-typescript-sdk';

const client = new Mix({
  serverURL: 'http://localhost:8088',
});

// Create a session
const session = await client.sessions.create({
  title: "My First Session"
});

// Send a message
const message = await client.messages.send({
  sessionId: session.id,
  content: "Hello from TypeScript SDK"
});

console.log("Session created:", session.id);
```

For more examples, see the [Mix TypeScript cookbook](https://github.com/recreate-run/mix-cookbooks)


## Setting Up the Client

```typescript
import { Mix } from 'mix-typescript-sdk';

const client = new Mix({
  serverURL: 'http://localhost:8088',
});
```

## Working with Sessions

### Creating a New Session

```typescript
async function createNewSession() {
  try {
    const session = await client.sessions.create({
      title: "My New Session"
    });
    console.log("Created session:", session.id);
    return session;
  } catch (error) {
    console.error("Failed to create session:", error);
    throw error;
  }
}
```

### Listing All Sessions

```typescript
async function listAllSessions() {
  try {
    const sessions = await client.sessions.list();
    console.log(`Found ${sessions.length} sessions`);
    return sessions;
  } catch (error) {
    console.error("Failed to list sessions:", error);
    throw error;
  }
}
```

### Getting a Specific Session

```typescript
async function getSessionById(sessionId: string) {
  try {
    const session = await client.sessions.get({
      id: sessionId
    });
    console.log("Retrieved session:", session.title);
    return session;
  } catch (error) {
    console.error(`Failed to get session ${sessionId}:`, error);
    throw error;
  }
}
```

### Deleting a Session

```typescript
async function deleteSession(sessionId: string) {
  try {
    await client.sessions.delete({
      id: sessionId
    });
    console.log(`Session ${sessionId} deleted successfully`);
  } catch (error) {
    console.error(`Failed to delete session ${sessionId}:`, error);
    throw error;
  }
}
```

## Selecting Provider and Model

```typescript
async function selectProviderAndModel() {
  try {
    // Get available providers and models
    const providers = await client.preferences.getAvailableProviders();
    console.log('Available providers:', Object.keys(providers));
    
    // Set preferred provider and model
    await client.preferences.updatePreferences({
      preferred_provider: 'anthropic',
      main_agent_model: 'claude-4-sonnet'
    });
    
    console.log('Provider and model selected successfully');
  } catch (error) {
    console.error('Failed to select provider and model:', error);
    throw error;
  }
}
```

## Working with Messages

### Sending a Message

```typescript
async function sendMessage(sessionId: string, content: string) {
  try {
    const message = await client.messages.send({
      sessionId,
      content
    });
    console.log("Message sent with ID:", message.id);
    return message;
  } catch (error) {
    console.error("Failed to send message:", error);
    throw error;
  }
}
```

### Getting Session Messages

```typescript
async function getSessionMessages(sessionId: string) {
  try {
    const messages = await client.messages.getSession({
      sessionId
    });
    console.log(`Retrieved ${messages.length} messages for session ${sessionId}`);
    return messages;
  } catch (error) {
    console.error(`Failed to get messages for session ${sessionId}:`, error);
    throw error;
  }
}